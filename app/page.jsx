'use client';

import { AnimatePresence, motion } from 'framer-motion';
import { ArrowLeft, ArrowRight, Camera, Check, History, Search, Send, Target, X, Zap, GitCompare, MessageCircleQuestion } from 'lucide-react';
import { useEffect, useMemo, useRef, useState } from 'react';
import { supabase } from '../lib/supabase';
import { ACTIONS, MAX_STEPS, START_TASK, generateNextTask, latestPayload, parsePayload, serializeStep, starterPayload } from '../lib/pulse-v2';

const HISTORY_KEY = 'pulse:v3:pulses';
const SESSION_KEY = 'pulse:v3:sessions';
const DEVICE_KEY = 'pulse:v4:creator-id';

function safeParse(value, fallback = null) { try { return JSON.parse(value); } catch { return fallback; } }
function stepsOf(relay) { return Array.isArray(relay?.steps) ? relay.steps : []; }
function readSessions() { if (typeof window === 'undefined') return {}; return safeParse(localStorage.getItem(SESSION_KEY) || '{}', {}); }
function readHistory() { if (typeof window === 'undefined') return []; return safeParse(localStorage.getItem(HISTORY_KEY) || '[]', []); }
function saveSession(id, value) { const all = readSessions(); all[id] = value; localStorage.setItem(SESSION_KEY, JSON.stringify(all)); }
function saveHistory(entry) { const next = [entry, ...readHistory().filter((item) => item.id !== entry.id)].slice(0, 30); localStorage.setItem(HISTORY_KEY, JSON.stringify(next)); }
function deviceId() { if (typeof window === 'undefined') return ''; let id = localStorage.getItem(DEVICE_KEY); if (!id) { id = crypto.randomUUID(); localStorage.setItem(DEVICE_KEY, id); } return id; }
function payloadsOf(relay) { return stepsOf(relay).map((step) => parsePayload(step?.output)).filter(Boolean); }
function taskForRelay(relay) { return latestPayload(relay)?.task || START_TASK; }
function artifactText(payload) { return payload?.result?.text || payload?.result?.note || payload?.result?.evidence || payload?.result?.summary || ''; }
function actionIcon(action) { if (action === ACTIONS.CHOOSE) return Target; if (action === ACTIONS.FIND || action === ACTIONS.CAPTURE) return Camera; if (action === ACTIONS.COMPARE) return GitCompare; if (action === ACTIONS.CHALLENGE) return MessageCircleQuestion; return Zap; }

async function imageFileToDataUrl(file, maxSide = 960, quality = 0.68) { if (!file?.type?.startsWith('image/')) throw new Error('画像を選んでください。'); const source = await createImageBitmap(file); const scale = Math.min(1, maxSide / Math.max(source.width, source.height)); const canvas = document.createElement('canvas'); canvas.width = Math.max(1, Math.round(source.width * scale)); canvas.height = Math.max(1, Math.round(source.height * scale)); const ctx = canvas.getContext('2d'); if (!ctx) throw new Error('画像を読み込めませんでした。'); ctx.drawImage(source, 0, 0, canvas.width, canvas.height); source.close(); return canvas.toDataURL('image/jpeg', quality); }
function PhotoFrame({ dataUrl, marker, onMark, compact = false }) { if (!dataUrl) return <div className={`grid aspect-[4/3] place-items-center rounded-[26px] border border-dashed border-[#24251f]/15 bg-[#f3efe5]/45 text-sm text-[#686b5b] ${compact ? 'min-h-28' : ''}`}>画像がここに表示されます</div>; return <div className={`relative aspect-[4/3] overflow-hidden rounded-[26px] bg-[#d6cdbb] ${onMark ? 'cursor-crosshair' : ''}`} onClick={(event) => { if (!onMark) return; const rect = event.currentTarget.getBoundingClientRect(); onMark({ x: ((event.clientX - rect.left) / rect.width) * 100, y: ((event.clientY - rect.top) / rect.height) * 100 }); }}><img src={dataUrl} alt="Pulse" className="block h-full w-full object-cover" />{marker ? <motion.div initial={{ scale: .3, opacity: 0 }} animate={{ scale: 1, opacity: 1 }} className="absolute h-12 w-12 -translate-x-1/2 -translate-y-1/2 rounded-full border-2 border-[#f8f4eb] bg-[#f8f4eb]/10 shadow-[0_0_0_5px_rgba(36,37,31,.26)]" style={{ left: `${marker.x}%`, top: `${marker.y}%` }} /> : null}</div>; }
function PrimaryButton({ children, onClick, disabled, icon: Icon = ArrowRight }) { return <button disabled={disabled} onClick={onClick} className="inline-flex items-center justify-center gap-3 rounded-full bg-[#24251f] px-6 py-3 text-sm font-semibold text-[#f3efe5] transition-transform hover:-translate-y-0.5 active:scale-[.98] disabled:pointer-events-none disabled:opacity-35">{children}<Icon size={17} /></button>; }
function StepPill({ action }) { const Icon = actionIcon(action); const label = ({ CAPTURE: '撮る', FIND: '探す', CHOOSE: '選ぶ', INTERPRET: '解釈する', COMPARE: '比べる', CHALLENGE: '疑う', PREDICT: '予測する' })[action] || action; return <div className="inline-flex items-center gap-2 rounded-full border border-[#24251f]/12 bg-[#f3efe5]/72 px-3 py-1.5 text-[11px] font-bold uppercase tracking-[.15em]"><Icon size={13} /> {label}</div>; }

export default function Page() {
  const [screen, setScreen] = useState('home'); const [relay, setRelay] = useState(null); const [role, setRole] = useState(''); const [token, setToken] = useState(''); const [busy, setBusy] = useState(false); const [error, setError] = useState(''); const [history, setHistory] = useState([]); const [showHistory, setShowHistory] = useState(false); const [creatorPhoto, setCreatorPhoto] = useState(''); const [photo, setPhoto] = useState(''); const [marker, setMarker] = useState(null); const [text, setText] = useState(''); const [compareNote, setCompareNote] = useState(''); const [claim, setClaim] = useState(''); const [challengeEvidence, setChallengeEvidence] = useState(''); const creatorInputRef = useRef(null);
  const payload = useMemo(() => latestPayload(relay), [relay]); const task = useMemo(() => taskForRelay(relay), [relay]); const count = relay?.step_count ?? stepsOf(relay).length; const revealSteps = useMemo(() => payloadsOf(relay), [relay]); const currentArtifact = payload?.artifact?.dataUrl || payload?.result?.dataUrl;
  useEffect(() => { setHistory(readHistory()); }, []);
  useEffect(() => { const entries = history.slice(0, 12); if (!entries.length) return undefined; const channels = entries.map((entry) => supabase.channel(`pulse-history-${entry.id}`).on('postgres_changes', { event: 'UPDATE', schema: 'public', table: 'relays', filter: `id=eq.${entry.id}` }, ({ new: next }) => { saveHistory({ id: next.id, role: entry.role, status: next.status, stepCount: next.step_count ?? stepsOf(next).length, updatedAt: Date.now() }); setHistory(readHistory()); if (relay?.id === next.id) setRelay(next); })).subscribe()); return () => { channels.forEach((channel) => supabase.removeChannel(channel)); }; }, [history, relay?.id]);
  useEffect(() => { if (!relay?.id) return undefined; const channel = supabase.channel(`pulse-v3-${relay.id}`).on('postgres_changes', { event: 'UPDATE', schema: 'public', table: 'relays', filter: `id=eq.${relay.id}` }, ({ new: next }) => { setRelay(next); saveHistory({ id: next.id, role, status: next.status, stepCount: next.step_count ?? stepsOf(next).length, updatedAt: Date.now() }); setHistory(readHistory()); if (next.status === 'complete') setScreen('result'); }).subscribe(); return () => { supabase.removeChannel(channel); }; }, [relay?.id, role]);
  function reset() { setScreen('home'); setRelay(null); setRole(''); setToken(''); setCreatorPhoto(''); setPhoto(''); setMarker(null); setText(''); setCompareNote(''); setClaim(''); setChallengeEvidence(''); setError(''); setShowHistory(false); }
  function openCreate() { setCreatorPhoto(''); setError(''); setScreen('create'); }
  async function onPhotoPicked(event, setter) { const file = event.target.files?.[0]; event.target.value = ''; if (!file) return; try { setBusy(true); setError(''); setter(await imageFileToDataUrl(file)); } catch (err) { setError(err?.message || '画像を読み込めませんでした。'); } finally { setBusy(false); } }
  async function createPulse() { if (!creatorPhoto) { setError('最初の写真を撮ってください。'); return; } setBusy(true); setError(''); try { const { data, error: dbError } = await supabase.rpc('create_relay', { p_seed: JSON.stringify(starterPayload(creatorPhoto, deviceId())) }); if (dbError) throw dbError; if (!data?.id) throw new Error('Pulseを作れませんでした。'); setRelay(data); setRole('creator'); setToken(''); saveSession(data.id, { role: 'creator', token: '' }); saveHistory({ id: data.id, role: 'creator', status: data.status, stepCount: data.step_count ?? 0, updatedAt: Date.now() }); setHistory(readHistory()); setScreen('waiting'); } catch (err) { setError(err?.message || 'Pulseを作れませんでした。'); } finally { setBusy(false); } }
  async function claimPulse() { setBusy(true); setError(''); try { const { data, error: dbError } = await supabase.rpc('claim_relay', { p_exclude_creator_id: deviceId() }); if (dbError) throw dbError; if (!data?.relay) { setError('今は参加できるPulseがありません。'); return; } setRelay(data.relay); setRole('stranger'); setToken(data.token); setPhoto(''); setMarker(null); setText(''); setCompareNote(''); setClaim(''); setChallengeEvidence(''); setScreen('task'); saveSession(data.relay.id, { role: 'stranger', token: data.token }); saveHistory({ id: data.relay.id, role: 'stranger', status: data.relay.status, stepCount: data.relay.step_count ?? 0, updatedAt: Date.now() }); setHistory(readHistory()); } catch (err) { setError(err?.message || 'Pulseを見つけられませんでした。'); } finally { setBusy(false); } }
  async function submitStep() { if (!relay || !token || busy) return; const action = task?.actionType; const inputType = task?.inputType; let result = {}; let nextArtifact = payload?.artifact || null;
    if (inputType === 'tap') { if (!marker) { setError('写真の中から1か所をタップしてください。'); return; } result = { marker, summary: '写真の中の1点を選択した' }; nextArtifact = { ...nextArtifact, marker }; }
    else if (inputType === 'text') { const clean = text.trim().replace(/\s+/g, ' '); if (!clean || clean.length > (task.maxLength || 120)) { setError('短い一文で入力してください。'); return; } result = { text: clean, summary: clean }; nextArtifact = { type: 'text', text: clean }; }
    else if (inputType === 'compare') { const note = compareNote.trim().replace(/\s+/g, ' '); if (!photo) { setError('比較するための新しい写真を1枚追加してください。'); return; } if (!note || note.length > 120) { setError('違いを一文で書いてください。'); return; } result = { dataUrl: photo, note, summary: note }; nextArtifact = { type: 'compare', previousDataUrl: currentArtifact || null, dataUrl: photo, note }; }
    else if (inputType === 'challenge') { const cleanClaim = claim.trim().replace(/\s+/g, ' '); const cleanEvidence = challengeEvidence.trim().replace(/\s+/g, ' '); if (!cleanClaim || cleanClaim.length > 120) { setError('疑っている主張を一文で書いてください。'); return; } if (!photo) { setError('反証になりそうなものを1枚撮ってください。'); return; } if (!cleanEvidence || cleanEvidence.length > 120) { setError('なぜ反証になるのか一文で書いてください。'); return; } result = { claim: cleanClaim, dataUrl: photo, evidence: cleanEvidence, summary: cleanEvidence }; nextArtifact = { type: 'challenge', dataUrl: photo, claim: cleanClaim, evidence: cleanEvidence }; }
    else { if (!photo) { setError('写真を1枚追加してください。'); return; } result = { dataUrl: photo, summary: '新しい写真を1枚残した' }; nextArtifact = { type: 'photo', dataUrl: photo, marker: null, text: null }; }
    const currentStep = count + 1; const previous = { ...payload, action, result, artifact: nextArtifact, task }; const actionHistory = [...payloadsOf(relay).map((item) => item.action).filter(Boolean), action]; const nextTask = currentStep >= MAX_STEPS ? null : generateNextTask({ previous, history: actionHistory, seed: relay.id, step: currentStep + 1 }); const output = serializeStep({ artifact: nextArtifact, action, result, step: currentStep, task, nextTask });
    setBusy(true); setError(''); try { const { data, error: dbError } = await supabase.rpc('submit_relay_step', { p_relay_id: relay.id, p_token: token, p_output: output }); if (dbError) throw dbError; setRelay(data); setToken(''); saveSession(relay.id, { role, token: '' }); saveHistory({ id: data.id, role, status: data.status, stepCount: data.step_count ?? stepsOf(data).length, updatedAt: Date.now() }); setHistory(readHistory()); setPhoto(''); setMarker(null); setText(''); setCompareNote(''); setClaim(''); setChallengeEvidence(''); setScreen(data.status === 'complete' || !nextTask ? 'result' : 'waiting'); } catch (err) { setError(err?.message || 'Pulseを次へ渡せませんでした。'); } finally { setBusy(false); } }
  async function resume(entry) { setBusy(true); setError(''); try { const { data, error: dbError } = await supabase.from('relays').select('*').eq('id', entry.id).maybeSingle(); if (dbError || !data) throw dbError || new Error('そのPulseはもうありません。'); const session = readSessions()[entry.id] || {}; setRelay(data); setRole(entry.role || session.role || 'creator'); setToken(session.token || ''); setShowHistory(false); setScreen(data.status === 'complete' ? 'result' : session.token ? 'task' : 'waiting'); } catch (err) { setError(err?.message || 'そのPulseを開けませんでした。'); } finally { setBusy(false); } }
  function renderInput() { const type = task?.inputType; if (type === 'tap') return <div className="space-y-4"><PhotoFrame dataUrl={currentArtifact} marker={marker} onMark={setMarker} /><p className="text-sm text-[#686b5b]">写真の上をタップ。選んだ場所が次の流れに残ります。</p></div>; if (type === 'text') return <div className="space-y-4"><div className="rounded-[26px] border border-[#24251f]/12 bg-[#f3efe5]/70 p-5"><div className="mb-3 flex items-center justify-between text-[11px] font-bold uppercase tracking-[.18em] text-[#686b5b]"><span>your interpretation</span><span>{text.length}/120</span></div><textarea value={text} onChange={(e) => setText(e.target.value)} maxLength={120} rows={5} placeholder={task.actionType === ACTIONS.PREDICT ? '次に見つかりそうなものを一文で予測' : '一文で残す'} className="w-full resize-none bg-transparent text-lg leading-8 outline-none placeholder:text-[#686b5b]/45" /></div></div>; if (type === 'compare') return <div className="space-y-4"><div className="grid grid-cols-2 gap-3"><PhotoFrame dataUrl={currentArtifact} compact /><label className="grid aspect-[4/3] cursor-pointer place-items-center overflow-hidden rounded-[26px] border border-dashed border-[#24251f]/15 bg-[#f3efe5]/45">{photo ? <img src={photo} alt="比較写真" className="h-full w-full object-cover" /> : <div className="text-center"><Camera className="mx-auto mb-2" size={24} /><span className="text-sm font-semibold">新しい写真</span></div>}<input type="file" accept="image/*" capture="environment" className="hidden" onChange={(e) => onPhotoPicked(e, setPhoto)} /></label></div><input value={compareNote} onChange={(e) => setCompareNote(e.target.value)} maxLength={120} placeholder="違いを一文で" className="w-full rounded-2xl border border-[#24251f]/12 bg-[#f3efe5]/70 px-5 py-4 outline-none" /></div>; if (type === 'challenge') return <div className="space-y-4"><div className="rounded-[24px] border border-[#24251f]/12 bg-[#f3efe5]/70 p-5"><p className="text-[11px] font-bold uppercase tracking-[.18em] text-[#686b5b]">CLAIM</p><p className="mt-2 text-base leading-6">{payload?.result?.text || payload?.result?.summary || '前の人が残した解釈'}</p></div><label className="grid aspect-[4/3] cursor-pointer place-items-center overflow-hidden rounded-[26px] border border-dashed border-[#24251f]/15 bg-[#f3efe5]/45">{photo ? <img src={photo} alt="反証写真" className="h-full w-full object-cover" /> : <div className="text-center"><Camera className="mx-auto mb-2" size={24} /><span className="text-sm font-semibold">反証になりそうなもの</span></div>}<input type="file" accept="image/*" capture="environment" className="hidden" onChange={(e) => onPhotoPicked(e, setPhoto)} /></label><input value={claim} onChange={(e) => setClaim(e.target.value)} maxLength={120} placeholder="疑っている主張を一文で" className="w-full rounded-2xl border border-[#24251f]/12 bg-[#f3efe5]/70 px-5 py-4 outline-none" /><input value={challengeEvidence} onChange={(e) => setChallengeEvidence(e.target.value)} maxLength={120} placeholder="なぜ反証になるか一文で" className="w-full rounded-2xl border border-[#24251f]/12 bg-[#f3efe5]/70 px-5 py-4 outline-none" /></div>; if (type === 'photo') return <div className="space-y-4"><label className="grid aspect-[4/3] cursor-pointer place-items-center overflow-hidden rounded-[26px] border border-dashed border-[#24251f]/15 bg-[#f3efe5]/45">{photo ? <img src={photo} alt="写真" className="h-full w-full object-cover" /> : <div className="text-center"><Camera className="mx-auto mb-3" size={28} /><p className="font-semibold">写真を1枚撮る</p></div>}<input type="file" accept="image/*" capture="environment" className="hidden" onChange={(e) => onPhotoPicked(e, setPhoto)} /></label></div>; return null; }
  return <main className="relative min-h-dvh overflow-hidden bg-[#e8e1d2] text-[#24251f]"><div className="pulse-grid" /><div className="pulse-grain" /><header className="absolute inset-x-0 top-0 z-50 flex items-center justify-between px-5 py-5 sm:px-9"><button onClick={reset} className="text-sm font-black tracking-[.28em]">PULSE</button><button onClick={() => setShowHistory(true)} aria-label="履歴" className="grid h-10 w-10 place-items-center rounded-full border border-[#24251f]/12 bg-[#f3efe5]/75"><History size={17} strokeWidth={1.7} /></button></header><AnimatePresence mode="wait">{screen === 'home' && <motion.section key="home" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} className="relative z-10 flex min-h-dvh items-center px-5 py-28 sm:px-10"><div className="mx-auto w-full max-w-6xl"><div className="grid gap-12 lg:grid-cols-[1.1fr_.9fr] lg:items-end"><div><p className="mb-5 text-[11px] font-black uppercase tracking-[.3em] text-[#686b5b]">one task. one human. then another.</p><h1 className="max-w-5xl text-balance text-[clamp(3.2rem,9vw,8rem)] font-semibold leading-[.84] tracking-[-.07em]">The world<br />changes hands.</h1><p className="mt-7 max-w-xl text-base leading-7 text-[#686b5b]">One person makes a move. A stranger inherits it, changes what happens next, and passes the change on.</p></div><div className="grid gap-3 sm:grid-cols-2 lg:pb-2"><button onClick={openCreate} className="group min-h-48 rounded-[30px] bg-[#667052] p-6 text-left text-[#f3efe5] transition-transform hover:-translate-y-1 active:scale-[.99]"><div className="flex items-start justify-between"><Camera size={22} /><ArrowRight className="transition-transform group-hover:translate-x-1" size={20} /></div><div className="mt-20 text-xl font-semibold tracking-[-.03em]">Start a Pulse</div><div className="mt-1 text-sm text-[#f3efe5]/65">Give a stranger the first move.</div></button><button onClick={claimPulse} disabled={busy} className="group min-h-48 rounded-[30px] border border-[#24251f]/12 bg-[#f3efe5]/78 p-6 text-left transition-transform hover:-translate-y-1 active:scale-[.99] disabled:opacity-40"><div className="flex items-start justify-between"><Target size={22} /><ArrowRight className="transition-transform group-hover:translate-x-1" size={20} /></div><div className="mt-20 text-xl font-semibold tracking-[-.03em]">Find a Pulse</div><div className="mt-1 text-sm text-[#686b5b]">Pick up where someone left off.</div></button></div></div></div></motion.section>}{screen === 'create' && <motion.section key="create" initial={{ opacity: 0, y: 14 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0 }} className="relative z-10 flex min-h-dvh items-center justify-center px-5 py-28 sm:px-9"><div className="grid w-full max-w-5xl gap-10 lg:grid-cols-[.85fr_1.15fr] lg:items-center"><div><button onClick={reset} className="mb-8 inline-flex items-center gap-2 text-xs font-bold uppercase tracking-[.2em] text-[#686b5b]"><ArrowLeft size={14} /> Back</button><p className="text-[11px] font-black uppercase tracking-[.3em] text-[#686b5b]">start</p><h1 className="mt-3 text-5xl font-semibold tracking-[-.05em]">Give it a<br />first move.</h1><p className="mt-5 max-w-md leading-7 text-[#686b5b]">You start the chain. After that, every stranger gets a different kind of action.</p></div><div className="rounded-[32px] bg-[#f3efe5]/72 p-3 shadow-[0_18px_60px_rgba(36,37,31,.08)]"><label className="grid aspect-[4/3] cursor-pointer place-items-center overflow-hidden rounded-[26px] bg-[#ddd4c3]">{creatorPhoto ? <img src={creatorPhoto} alt="最初の写真" className="h-full w-full object-cover" /> : <div className="text-center"><Camera className="mx-auto mb-3" size={28} /><p className="font-semibold">最初の写真を撮る</p><p className="mt-1 text-sm text-[#686b5b]">人や危険な場所は避けてください。</p></div>}<input ref={creatorInputRef} type="file" accept="image/*" capture="environment" className="hidden" onChange={(e) => onPhotoPicked(e, setCreatorPhoto)} /></label><div className="flex items-center justify-between gap-3 p-4"><StepPill action={ACTIONS.CAPTURE} /><PrimaryButton onClick={createPulse} disabled={!creatorPhoto || busy} icon={Send}>{busy ? 'Starting…' : 'Start Pulse'}</PrimaryButton></div></div></div></motion.section>}{screen === 'task' && <motion.section key="task" initial={{ opacity: 0, y: 16 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0 }} className="relative z-10 min-h-dvh px-5 pb-20 pt-28 sm:px-9"><div className="mx-auto max-w-3xl"><div className="mb-8 flex items-end justify-between gap-4"><div><p className="text-[11px] font-black uppercase tracking-[.3em] text-[#686b5b]">your turn · {Math.min(count + 1, MAX_STEPS)} / {MAX_STEPS}</p><h1 className="mt-3 text-[clamp(2.5rem,7vw,5rem)] font-semibold leading-[.9] tracking-[-.06em]">{task?.title}</h1></div><StepPill action={task?.actionType} /></div><div className="rounded-[34px] border border-[#24251f]/10 bg-[#f3efe5]/55 p-4 sm:p-6"><div className="mb-5 rounded-[24px] border border-[#24251f]/10 bg-[#f3efe5]/72 p-5"><p className="text-[11px] font-black uppercase tracking-[.22em] text-[#686b5b]">PREVIOUS MOVE</p>{payload?.artifact?.dataUrl ? <div className="mt-4"><PhotoFrame dataUrl={payload.artifact.dataUrl} compact /></div> : null}<div className="mt-4"><p className="text-[11px] font-bold uppercase tracking-[.18em] text-[#686b5b]">THEIR TASK</p><p className="mt-2 text-sm leading-6">{payload?.performedTask?.prompt || (payload?.step === 0 ? START_TASK.prompt : '前の人の一手')}</p>{artifactText(payload) ? <><p className="mt-4 text-[11px] font-bold uppercase tracking-[.18em] text-[#686b5b]">WHAT THEY LEFT</p><p className="mt-2 text-base leading-6">{artifactText(payload)}</p></> : null}</div></div><div className="mb-7 rounded-[24px] bg-[#667052] p-6 text-[#f3efe5]"><p className="text-[11px] font-black uppercase tracking-[.22em] text-[#f3efe5]/55">YOUR TASK</p><p className="mt-3 text-xl leading-8 tracking-[-.02em]">{task?.prompt}</p><p className="mt-4 text-sm text-[#f3efe5]/65">{task?.hint}</p></div>{renderInput()}<div className="mt-6 flex items-center justify-between gap-4"><p className="max-w-xs text-xs leading-5 text-[#686b5b]">あなたの行動だけが、次のTaskを変えます。</p><PrimaryButton onClick={submitStep} disabled={busy} icon={ArrowRight}>{busy ? 'Passing…' : 'Pass it on'}</PrimaryButton></div></div></div></motion.section>}{screen === 'waiting' && <motion.section key="waiting" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} className="relative z-10 flex min-h-dvh items-center justify-center px-5 py-28"><div className="max-w-xl text-center"><div className="mx-auto mb-8 grid h-20 w-20 place-items-center rounded-full border border-[#24251f]/12 bg-[#f3efe5]/70"><span className="h-2.5 w-2.5 animate-pulse rounded-full bg-[#667052]" /></div><p className="text-[11px] font-black uppercase tracking-[.3em] text-[#686b5b]">pulse is moving</p><h1 className="mt-4 text-5xl font-semibold tracking-[-.06em]">Someone else<br />has the next move.</h1><p className="mt-5 leading-7 text-[#686b5b]">5 people will shape this chain. When the next action lands, it will be different because of what just happened.</p><div className="mt-8 flex justify-center"><PrimaryButton onClick={reset} icon={ArrowLeft}>Back home</PrimaryButton></div></div></motion.section>}{screen === 'result' && <motion.section key="result" initial={{ opacity: 0, y: 12 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0 }} className="relative z-10 min-h-dvh px-5 pb-20 pt-28 sm:px-9"><div className="mx-auto max-w-5xl"><p className="text-[11px] font-black uppercase tracking-[.3em] text-[#686b5b]">REVEAL · {revealSteps.length + 1} actions</p><h1 className="mt-4 text-[clamp(3rem,8vw,7rem)] font-semibold leading-[.88] tracking-[-.07em]">5 strangers.<br />1 starting point.<br />Look what changed.</h1><p className="mt-7 max-w-xl leading-7 text-[#686b5b]">The chain is finished. The interesting part was every move that changed what came next.</p><div className="mt-12 space-y-4">{revealSteps.map((item, index) => <motion.div key={`${item.step}-${index}`} initial={{ opacity: 0, y: 12 }} animate={{ opacity: 1, y: 0 }} className="rounded-[28px] border border-[#24251f]/10 bg-[#f3efe5]/62 p-5"><div className="flex items-center justify-between"><span className="text-[11px] font-black uppercase tracking-[.2em] text-[#686b5b]">step {index + 1}</span><StepPill action={item.action} /></div><p className="mt-4 text-xs uppercase tracking-[.15em] text-[#686b5b]">THEIR TASK</p><p className="mt-2 text-sm leading-6">{item.performedTask?.prompt || '—'}</p>{item.artifact?.type === 'compare' ? <div className="mt-4 grid grid-cols-2 gap-2"><PhotoFrame dataUrl={item.artifact.previousDataUrl} compact /><PhotoFrame dataUrl={item.artifact.dataUrl} compact /></div> : item.artifact?.dataUrl ? <div className="mt-4"><PhotoFrame dataUrl={item.artifact.dataUrl} compact /></div> : null}<p className="mt-4 text-base leading-6">{artifactText(item)}</p></motion.div>)}</div><div className="mt-10"><PrimaryButton onClick={reset} icon={ArrowLeft}>Start another Pulse</PrimaryButton></div></div></motion.section>}</AnimatePresence>{error ? <div className="fixed inset-x-0 bottom-5 z-[70] flex justify-center px-5"><div className="rounded-full border border-[#ad735c]/20 bg-[#f3efe5] px-5 py-3 text-sm shadow-lg">{error}</div></div> : null}<AnimatePresence>{showHistory ? <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} className="fixed inset-0 z-[80] bg-[#24251f]/20 p-4 backdrop-blur-sm" onClick={() => setShowHistory(false)}><motion.div initial={{ y: 18, opacity: 0 }} animate={{ y: 0, opacity: 1 }} exit={{ y: 18, opacity: 0 }} onClick={(e) => e.stopPropagation()} className="mx-auto mt-14 max-w-lg rounded-[30px] bg-[#f3efe5] p-5 shadow-2xl"><div className="flex items-center justify-between"><div><p className="text-[11px] font-black uppercase tracking-[.2em] text-[#686b5b]">history</p><h2 className="mt-1 text-2xl font-semibold">Your Pulses</h2></div><button onClick={() => setShowHistory(false)} className="grid h-10 w-10 place-items-center rounded-full border border-[#24251f]/12"><X size={16} /></button></div><div className="mt-5 space-y-2">{history.length ? history.map((entry) => <button key={entry.id} onClick={() => resume(entry)} className="flex w-full items-center justify-between rounded-2xl border border-[#24251f]/10 bg-white/30 p-4 text-left"><span><span className="block text-sm font-semibold">{entry.role === 'creator' ? 'You started it' : 'You joined it'}</span><span className="block text-xs text-[#686b5b]">{entry.status} · {entry.stepCount ?? 0} moves</span></span><ArrowRight size={16} /></button>) : <p className="py-8 text-center text-sm text-[#686b5b]">まだPulseはありません。</p>}</div></motion.div></motion.div> : null}</AnimatePresence></main>;
}
